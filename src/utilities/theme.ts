import { extendTheme } from "@chakra-ui/react";

const colors = {
  primary: "#424a54",
  highlight: "#cddc39",
  back: "#f5f5f5",
  white: "#fff",
  label: "#4d4d4d",
  black: "#000",
  widget: "#F2F4F6",
};

const theme = extendTheme({ colors });

export default theme;
